services:
  db:
    image: postgres:16
    container_name: odoo-booking_module
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: odoo
    ports:
      - "5437:5432"
    volumes:
      - odoo-booking_module-db-data:/var/lib/postgresql/data
    restart: unless-stopped

  odoo:
    image: odoo:18.0
    container_name: odoo-booking_module-web
    depends_on:
      - db
    ports:
      - "8073:8069"
    environment:
      HOST: db
      USER: odoo
      PASSWORD: odoo
    volumes:
      - odoo-booking_module-web-data:/var/lib/odoo
      - ./addons:/mnt/extra-addons
      - ./config:/etc/odoo
    restart: unless-stopped

volumes:
  odoo-booking_module-db-data:
  odoo-booking_module-web-data: